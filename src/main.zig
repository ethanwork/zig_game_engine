const examples = @import("zig_examples/examples.zig");

pub fn main() !void {
    //try examples.examples_main();
    var done = false;
    while (!done) {
        // infinite loop
        done = true;
    }
    return;
}
